#include <stdio.h>
#include <math.h>
int main()
{
    // int a,b,result;
    // scanf("%d%d",&a,&b);
    // result = pow(a,b);//计算a的b次方
    // printf("result = %d",result);
    int a=pow(5,0.3);
    printf("%3d",5);
    printf("%4s","abc");
    return 0;
}